Lightweight player for the tracks in this repo. Needs a static server, not file://

    cd player
    python -m http.server 8000

then open http://localhost:8000 in Chrome or Edge. Built from examples/terminal-repl in the strudel repo.
